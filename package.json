{
  "name": "vibestudio",
  "private": true,
  "version": "0.1.0",
  "description": "VibeStudio — natural-language prompt → validated Process IR → BPMN + spec kit → graph-engineering dev plan",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w @vibestudio/ir -w @vibestudio/bpmn -w @vibestudio/speckit && npm run build -w @vibestudio/web",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build:libs": "npm run build -w @vibestudio/ir -w @vibestudio/bpmn -w @vibestudio/speckit",
    "dev": "npm run build:libs && npm run dev -w @vibestudio/web"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
